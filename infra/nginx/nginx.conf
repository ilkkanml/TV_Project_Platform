# Placeholder Nginx configuration for future deployment.
# Do not use this file as production-ready configuration without review.

events {}

http {
  server {
    listen 8080;

    location /health {
      return 200 "ok\n";
    }
  }
}
